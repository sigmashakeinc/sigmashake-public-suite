# Agent Notes

- Keep the mirror public-safe and allowlist-only.
- Do not add OBS runtime/config artifacts, local paths, credentials, logs,
  recordings, captures, or streamer-private service internals.
- Run `bun run test` and `bun run test:sast` before proposing public changes.
